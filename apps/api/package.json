{
  "name": "@alphapump/api",
  "version": "0.0.0",
  "private": true,
  "description": "Backend Hono: REST, synchronizacja, LLM — etapy 3, 4, 11, 12, 14.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
