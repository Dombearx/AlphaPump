/**
 * Tokeny API — wydawanie i unieważnianie kluczy do API poza aplikacją.
 */

import { ApiKeysScreen } from '../src/screens/api-keys';

export default function ApiKeysRoute() {
  return <ApiKeysScreen />;
}
