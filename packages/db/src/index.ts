/**
 * Schematy danych AlphaPump — jeden opis schematu, dwa dialekty
 * (PostgreSQL po stronie serwera, SQLite po stronie telefonu).
 *
 * Zawartość powstaje w etapie 2 planu implementacji.
 */

export const PACKAGE_NAME = '@alphapump/db';
