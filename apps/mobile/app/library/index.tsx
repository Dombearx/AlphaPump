/**
 * Biblioteka ćwiczeń — przeglądanie i filtrowanie po tagach.
 *
 * Trasa nie robi nic poza wskazaniem ekranu: cały widok jest w `src/screens`,
 * tak jak reszta aplikacji.
 */

import { LibraryScreen } from '../../src/screens/library';

export default function LibraryRoute() {
  return <LibraryScreen />;
}
